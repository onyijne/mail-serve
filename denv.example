# main database
DB_HOST=if_ms_mysql
DB_PORT=3306
DB_DATABASE=divine_apps
DB_USERNAME=db_user
DB_PASSWORD=paswer56bfe
DATABASE_URL=mysql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_DATABASE}

# ports
FROXLOR_PORT=8081
ROUNDCUBE_PORT=8082

# docker image build
APP_ROOT="/var/www"
SYSUSER=
SYSID=

# supervisor
QUEUE_CONNECTION=sync
QUEUE_OPTIONS="--queue=default --sleep=3 --tries=3"
NUM_PROCS=1
SUPERVISOR_PORT=3993
SUPERVISOR_USERNAME=teamusers
SUPERVISOR_PASSWORD=ci2904i8hg43d

# MYSQL
MYSQL_ROOT_PASSWORD=kj3io4oJuih8jLE4f
MYSQL_DATABASE=${DB_DATABASE}
MYSQL_PASSWORD=${DB_PASSWORD}
MYSQL_USER=${DB_USERNAME}
MYSQL_HOST=${DB_HOST}

# phpmyadmin
PMA_ARBITRARY=0
PMA_PORT=${DB_PORT}
PMA_HOST=${DB_HOST}
PHPMYADMIN_PORT=3826

# smtp
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=''
SMTP_ENCRYPTION='tls'
USE_FILE_TRANSPORT=false
