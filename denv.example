# main database
DB_HOST=cf_ms_mysql
DB_PORT=3306
DB_DATABASE=emails
DB_USERNAME=emails_admin
DB_PASSWORD=D89466226d04f7B
DATABASE_URL=mysql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_DATABASE}

# ports
FROXLOR_PORT=8081
ROUNDCUBE_PORT=8082

# docker image build
APP_ROOT="/var/www"
SYSUSER=
SYSID=

# supervisor
QUEUE_CONNECTION=sync
QUEUE_OPTIONS="--queue=default --sleep=3 --tries=3"
NUM_PROCS=1
SUPERVISOR_PORT=3993
SUPERVISOR_USERNAME=teamusers
SUPERVISOR_PASSWORD=R171a673c033P

# MYSQL
MYSQL_ROOT_PASSWORD=W255d72188K
MYSQL_DATABASE=${DB_DATABASE}
MYSQL_PASSWORD=${DB_PASSWORD}
MYSQL_USER=${DB_USERNAME}
MYSQL_HOST=${DB_HOST}

# smtp
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=''
SMTP_ENCRYPTION='tls'
USE_FILE_TRANSPORT=false
